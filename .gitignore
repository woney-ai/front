# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vendored agent skills — reproducible from skills-lock.json
.agents/
.claude/skills/
.atl/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI scratch
supabase/.temp/
.vercel

# Real env files stay out; the example is tracked on purpose so a fresh clone
# knows which keys it needs. `vercel link` writes a broad `.env*` here, which
# would also swallow that example.
.env
.env.local
.env.*.local
