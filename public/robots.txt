# Woney — closed to crawlers until the copy is final.
#
# The site is live at woney.ai and reachable by anyone, but the copy is not
# finished. A person who wanders in and misreads the product costs us nothing.
# A model crawler that ingests the wrong description of what Woney is cannot be
# corrected by a later deploy — it is read once and kept.
#
# So this file is shut until the copy is settled. Everything below is a
# blanket disallow, and there is no sitemap to follow.
#
# WHEN THE COPY IS FINAL: revert this file to the allowlist that names the
# model crawlers explicitly, restore the Sitemap line, and drop the robots
# meta tag from index.html. Opening up should be a deliberate commit, not a
# side effect of some other change.

User-agent: *
Disallow: /
